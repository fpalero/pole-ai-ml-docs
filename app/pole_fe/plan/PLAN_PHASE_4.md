# Fase 4 — Tricks detail: Video Editor + Shift — ✅ DONE

> Plan maestro: [PLAN.md](PLAN.md)

## Tareas

- [x] Playable clip editor (controles de video via ElementRef), trim (START/END + frame steps), crop & shift (X/Y/SCALE), preview toggle, temporal previews, undo/redo (max 20), metadata tab + label, Accept (`cutter_config` persisted) / Discard / Reset.
- [x] Two-phase clip shift + Save Frame; shared batch-upload; thumbnail generation for clips.
- [x] Unit tests T4.x (UC-30/32/33/34).

## Criterios de aceptación

- [x] Editor de clip + crop/shift funcional; tests T4.x verdes.