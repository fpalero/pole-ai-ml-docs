# Fase 19 — Stitch Analysis-Detail parity round 2 (PO requirements 2026-08-23) — 📋 PLANNED

> Plan maestro: [PLAN.md](../PLAN.md) · Origen: revisión del PO tras merge de Phase 17 —
> "la Detail Page no se parece a Stitch". Requisitos dictados por el PO prevalecen sobre el
> export HTML donde difieran.

## Contexto

The detail-page tabs predate the new Stitch screen; Phase 17 did partial parity only. The PO
dictated the target behavior per tab:

| Tab | PO requirement | Data source | Class |
| :--- | :--- | :--- | :--- |
| Statistics | **Spider/radar diagram with the 5 metrics** + description of what each metric means + explanation generated by the agent | summary per-metric scores (0–100) ✓; persisted `coach_summary` content ✓ | FE-only |
| Pose Data | **List of poses**, each annotated with its coach insights (correct/needs-adjustment/how-to-improve); skeleton accent visual (green=correct / red=adjust per design SVG) | detections/frames ✓; coach_insights ✓ | FE-only |
| Plan | Auto plan for the **already-detected trick** — no manual trick input when `trick_label` exists (manual entry only as fallback) | `trick_label` on video doc ✓; POST /coach-plan is generate-or-cached | FE-only |
| Sidebar | Upload button present/visible per design (report says missing — investigate responsive/state hiding) | existing `upload-btn` | FE-only |

No new endpoints required — all data already exists (coach endpoints merged #100–#116).

## Tickets

| Ticket | Scope |
| :--- | :--- |
| `PAIML-POLE-ANALYST-064` | Statistics tab redesign: radar (5 metrics) + metric legend + agent explanation |
| `PAIML-POLE-ANALYST-065` | Pose Data tab: annotated pose list w/ insights + skeleton accents |
| `PAIML-POLE-ANALYST-066` | Plan auto-generation for detected trick |
| `PAIML-POLE-ANALYST-067` | Sidebar upload visibility/placement investigation + fix |

## Quality gates

- Per-ticket **≥3 iteration cycles**: developer → QA/design-review vs Stitch contract → fixes →
  re-review (QA suggests improvements even with no mismatches). Cap: stop early only if QA finds
  zero mismatches twice consecutively.
- After all tickets: integration tests (`pixi run pole-analyst-e2e`, targeted `test-api`).

## Dependencies

- Builds on Phases 16/17 (merged). No cross-ticket blockers except shared files
  (`summary-tab` untouched by these; `detail page` touched by 064–066 → land sequentially or
  coordinate).
